{
  "name": "@mre-delegate-fail/mkBlueprint",
  "version": "0.0.1",
  "description": "mk blueprint from aiken output to lucid input",
  "main": "src/index.ts",
  "scripts": {
    "build": "rm -rf dist && tsc --build"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public",
    "main": "dist/index.js",
    "typings": "dist/src/index.d.ts"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "typescript": "^5.7.2"
  }
}
